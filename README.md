# tphone
it's in c. it's in raylib. it's in sip. it's in rest. it's probably in some more stuff...


my_raylib_project/
├── src/
│   └── main.c
├── libs/
│   └── raylib/               # Pre-built static Raylib libs + headers
├── include/                 # Optional: for your own headers
├── build/                   # Output directory
└── Makefile (or CMakeLists.txt)